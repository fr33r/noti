/**
 * Contains the constructs that serve as RESTful resource representations that are serialized and
 * deserialized as the {@code application/xml} media type, as well as the types responsible for
 * creating these representations.
 */
package api.representations.xml;
